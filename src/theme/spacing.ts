/**
 * Spacing scale shared across screens and components.
 */
export const spacing = {
  xs: 4,
  sm: 8,
  md: 12,
  base: 16,
  lg: 20,
  xl: 24,
  xxl: 32,
} as const;

export type Spacing = typeof spacing;
