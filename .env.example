# The deployed API origin is public configuration, not a secret.
EXPO_PUBLIC_API_BASE_URL=https://m4.blocktype.cl

# Development-only fixture navigation. This is honored only when __DEV__ is true.
# Production builds always use the real authentication flow.
EXPO_PUBLIC_USE_FIXTURES=false
